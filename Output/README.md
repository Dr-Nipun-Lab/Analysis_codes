# Analysis Outputs

This directory is intended for the output files generated by the analysis scripts in the `scripts/` directory.

The repository does not include study-specific output files or patient-level data because of data-sharing and confidentiality considerations.

The analysis scripts generate tables, statistical results, and visualizations corresponding to the analyses described in the associated study.
